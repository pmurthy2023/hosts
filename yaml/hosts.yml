apiVersion: v1
kind: Pod
metadata:
  name: hosts
  labels:
    name: hostselect
spec:
  containers:
  - name: hosts
    image: pramurthy/hosts:v1
    imagePullPolicy: Always
    command: [ "/bin/sh" ]
    args: [ "-c", "while true; do echo hello; sleep 10;done" ]
    ports:
    - name: app-port
      containerPort: 8000
